% proclinec.m:  read a line of text and return the number of 'words' and% the list of words.% Usage: [word_list, numwords] = proclinec(input_text,delim)% Written by:  Jonathan Jacobs%              July 1997  (last mod: 07/02/97)function [word_list, numwords] = proclinec(input_text,delim)if nargin == 0   help proclinec   returnendword_list=[];numwords=0;if isempty(input_text); return; endif nargin < 2   delim = {char(9),char(32),char(13)}; % whitespace charactersendcount = 1;while( ~all(isspace(input_text)) )   [temp, input_text] = strtok(input_text,delim);   word_list{count} = temp;   count = count + 1;endnumwords = count-1;